# Ulsan Open Transit — 재현용 의존성
# 설치: pip install -r requirements.txt
#
# ⚠️ 접근성 분석(accessibility.py / diagnose_access.py)은 r5py가 필요하며
#    r5py는 OpenJDK 21 이상이 있어야 동작합니다 (Java 8/11/17 불가).
#    - Ubuntu: sudo apt install openjdk-21-jdk
#    - macOS:  brew install openjdk@21
#    수집·시각화(pull_*, collect_*, make_access_map, build_dataset)는 Java 불필요.

# --- 데이터 처리 ---
pandas>=2.0
numpy>=1.24

# --- 지리/공간 ---
geopandas>=0.14
shapely>=2.0
folium>=0.15

# --- 접근성 라우팅 (OpenJDK 21 필요) ---
r5py>=0.1.0

# --- 시각화 ---
matplotlib>=3.7

# --- 설정 파싱 ---
PyYAML>=6.0

# 표준 라이브러리(별도 설치 불필요): json, sys, pathlib, datetime,
# urllib, collections, concurrent
